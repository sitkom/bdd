{% extends 'base_admin.html.twig' %}

{% block title %}Edit Fiche{% endblock %}

{% block body %}
    <h1>Edit Fiche</h1>

    {{ include('fiche/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('fiche_index') }}">back to list</a>

    {{ include('fiche/_delete_form.html.twig') }}
{% endblock %}
